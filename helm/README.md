Helm chart skeleton for deployment.

Add templates and values.yaml per your cluster conventions. Recommended resources:
- Deployment for monolith (app + worker as separate deployment entries)
- Service, Ingress, ConfigMap, Secret for K8s
- Values for replicas, resource requests/limits, environment overrides
