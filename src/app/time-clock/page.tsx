// Public time-clock kiosk — accessible without authentication
// Re-exports the same kiosk experience as the dashboard version
// but at /time-clock (no auth, no sidebar)
export { default } from "@/app/dashboard/time-clock/page";
