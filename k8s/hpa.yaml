apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: qc-monolith-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: qc-monolith
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 60
